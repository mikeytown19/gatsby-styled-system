import styled from 'styled-components'
import {
  space,
  color,
  typography
} from "styled-system";
import theme from '../../styles/theme'

export const Text = styled.div`
  ${typography}
  ${space}
  ${color}
`